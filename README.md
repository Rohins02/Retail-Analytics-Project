**VERY IMPORTANT PLEASE READ**

The web server some times gets overloaded and I have checked the logs and did all troubleshooting trying to optimize code. If does either of these following things please reach out to me so we can figure this out together: 
- Webapp takes too long to load which either leads to timeout or internal server error
- Webapp can't query db due to authentication issues and uses fallback values
- I have also provided screenshots of my End-to-End testing so if you would like to refer that also it works
